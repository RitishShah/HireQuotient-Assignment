const utils = require('../utils/response');

exports.createCommentValidations = (req, res, next) => {
    const bodyData = req.body;

    next();
};

exports.createPostValidations = (req, res, next) => {
    const bodyData = req.body;

    next();
};